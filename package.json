{
  "name": "obsidian-daily-dashboard",
  "version": "0.3.0",
  "private": true,
  "description": "Obsidian plugin with a dedicated dashboard tab for daily tracking, wallpaper styling, and automatic reports.",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "deploy": "node scripts/deploy-plugin.mjs",
    "build:deploy": "node scripts/build-deploy.mjs",
    "dev": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.17.30",
    "esbuild": "^0.25.3",
    "obsidian": "^1.8.10",
    "tslib": "^2.8.1",
    "typescript": "^5.8.3"
  }
}